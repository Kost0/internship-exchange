export default function SkillTag({ label }) {
    return (
        <span style={{ border: '1px solid #ccc', fontSize: 11, padding: '2px 8px', display: 'inline-block' }}>
            {label}
        </span>
    )
}